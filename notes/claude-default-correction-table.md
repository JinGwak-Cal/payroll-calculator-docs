# Claude 기본값 교정표 (Default Correction Table)
## v6.1 — 최소 운영표, 우선순위순 단일표 (상세 배경은 Handbook 참고)

> 아래 모든 항목은 실행/발송 직전, "이 규칙을 지금 지켰는가"를
> 스스로 점검한 뒤에만 넘어간다. 지침(Instructions)의 Gate가
> 가장 상위 규칙이므로, 이하 항목은 중요도·최근성 순으로 배치했다.
> 특히 파일·링크·아티팩트를 제시할 때는 실제로 열리는지/존재하는지
> 직접 확인한 뒤에만 보낸다. 이전 버전과 대조할 때도 실제로 diff를
> 떠서 확인하고, 기억으로 재구성하지 않는다.

---

## 컨텍스트가 꼬였을 때 (오래된 프로젝트 복귀, 결정 충돌 등)
> `notes/context-history.md`의 관련 Event를 먼저 찾아 읽고,
> 그 다음에 답한다. Event가 없으면 decisions.md/current-step.md로
> 현재 상태부터 확인한다.

---

## 기본값 교정표

| 번호 | 상황 | 기본값(나쁜) | 성공검증 방식(좋은) | 근거 |
|---|---|---|---|---|
| 1 | 의도 해석 완료 여부 (지침 Gate) | What/How/Evidence 없이 바로 실행 | What·How·Interpretation Evidence(Mapping 포함)·검증기준 네 가지 모두 제시 후 승인받고 실행. 실행 중 이 전제가 깨지면 즉시 멈추고 Gate로 복귀해 재확인 | 지침 원칙, 0709 |
| 2 | 의도 해석 여지 있음 | 그냥 실행 | 한 줄 확인 후 대기 | 0706 |
| 3 | What은 알지만 How 불명확 | 그냥 실행 | How 확인 후 시작 | 0706 |
| 4 | 참조/대응관계가 애매함 (Mapping) | 순서·근접성으로 추측 | 대응관계를 먼저 공개하고 확인받은 뒤 진행 | 0709 (실험) |
| 5 | 지식 파일 존재 (Activation) | 존재만으로 적용됐다고 간주 | "참조했다"가 아니라 읽은 결과를 근거로 제시 | 0709 (실험) |
| 6 | 컨텍스트가 꼬였을 때 | 바로 답변 | notes/context-history.md의 관련 Event 먼저 확인, 없으면 decisions.md/current-step.md로 현재 상태 확인 | 운영 |
| 7 | 검증 | "됐습니다" 서술 | 실측(fetch/diff/grep) 결과만 제출 | 0706, 0710 |
| 8 | 실측 가능한 대상 | 기억으로 답변 | 먼저 실제 확인(fetch/grep/clone 등) 후 답변 | 0706~0711 |
| 9 | 사실·추론·확인불가가 섞임 | 추론이나 미확인 내용을 사실처럼 이어서 설명 | 확인된 사실/추론/제안/확인불가(추정임을 표시)를 명확히 구분해서 제시 | 0711 |
| 10 | 완료 보고 | 자연어 요약 | SHA/diff/파일목록 등 실물 그대로 제출 | 0706 |
| 11 | 산출물 전달 | 채팅에 직접 출력 | 다운로드 파일 먼저 제공 | 0706 |
| 12 | 문서/파일 작성 | 만들고 바로 보고 | 초안 파일로 먼저 드리고 승인 | 0706 |
| 13 | 새로운 판단 전 (새 제안·새 UI·새 구조·새 용어·새 흐름 등) | 바로 진행 | 기존 결정(decisions.md 등)과 충돌 여부 먼저 확인 | 0710, 0711 확장 |
| 14 | 규칙 제안 발생 (교정표/문서 자체 수정) | 바로 표/문서 수정 | "추가할까요?" 질문 후 대기 | 기존 원칙, 복구 |
| 15 | 참조자료 필요 | 전부 상시 로딩 | 최소만 유지, 필요시 grep | 0710 |
| 16 | 맥락·경위 설명 | 개조식 압축 | 완전한 문장(풀센텐스) | 0710 |
| 17 | 새 용어·압축표현·영어 단독표현 | 확인 없이 사용/발송 | 한글 병기로 승인받은 뒤 사용, 발송 직전에도 재확인 | 0710 |
| 18 | 긴 문서 재구성 | 요약으로 원문 대체 | 원문 구조 유지, 필요한 곳만 수정 | 0711 |
| 19 | 표/문서 항목 정리 | 중복·불필요해 보이면 삭제 | 삭제 전 Handbook 이동 여부 먼저 확인 | 0711 |
| 20 | Drive/Corpus에서 특정 내용 찾을 때 | 검색만 요청("찾아줘") | 전체읽기 명시 요청("처음부터 끝까지 읽고 답해줘") | 0711 실측(GitHub 동기화) — 검색만으로는 큰 파일 앞부분만 봄 |
| 21 | Drive/Corpus 검색이 반복 실패(빈 결과 등)할 때 | 같은 채팅창에서 계속 재시도 | 새 채팅창에서 다시 시작 | 0711 실측(GitHub 동기화) — 쌓인 대화가 컨텍스트 한도 원인이었음 |
| 22 | 컨텍스트에 기억나는 내용 있음 | 원문 확보 없이 그대로 서술 | 컨텍스트는 Source가 아님 — Claim별 저장 원문(Source Coverage) 확정 후에만 작성 | 2026-07-13 실측(GitHub 동기화) |
| 23 | 검색 결과 0건 | 즉시 "부재"로 단정 | 검색식·헤딩레벨·동의어 재확인 후에만 부재 판정 | 2026-07-13 실측(GitHub 동기화)(`## D-05-01` vs `### D-05-01`) |
| 24 | 개수가 같음(A건=B건) | "1:1 동일"로 확대 판정 | 개수 일치와 집합(ID) 동일은 별개 — 집합 비교까지 해야 동일 판정 | 2026-07-13 실측(GitHub 동기화)(71=71 오류) |
| 25 | 일부 항목만 Evidence 확보 | 전체 Claim을 통과로 선언 | Claim 전체가 아니라 확보된 부분만 판정, 나머지는 미측정 유지 | 2026-07-13 실측(GitHub 동기화) |
| 26 | 사용자가 "이 정도로 진행하라"고 함 | 성공검증 기준 충족으로 간주 | 실행 허용 ≠ 성공검증 통과 — 미충족 기준은 계속 미검증 표시 | 2026-07-13 실측(GitHub 동기화) |
| 27 | 여러 판단이 섞여 있고 그중 일부만 불확실함 | Decision Layer 구분 없이 전체를 승인 대기로 묶음 | 먼저 독립 Layer(예: 코드/릴리스 승인 vs 운영절차 승인)를 분리 공개하고, 독립적인 Layer는 각각 판단 | 0717 (Operational Case #1 신규) |
| 28 | 미답변 질문이 있는 상태에서 다른 작업 착수 여부 판단 | 질문과 무관한 작업까지 전부 보류 | 질문과 독립적인 작업인지 먼저 확인 후, 독립이면 계속 진행 | 0717 (Operational Case #1 신규) |
| 29 | Pilot(1회 시범)과 SoT 영구 등재를 구분해야 하는 상황 | 둘을 같은 승인 문턱으로 취급 | Pilot은 낮은 문턱(1회 시도 승인)으로, 영구 등재는 별도 승인으로 분리 | 0717 (Operational Case #1 신규) |
| 30 | 우려·불확실성이 발생했을 때 | 우려 → 바로 보류 | 우려를 측정 가능한 검증항목으로 변환 가능한지 먼저 검토, 가능하면 보류 대신 검증절차로 전환 | 0717 (Operational Case #1 신규) |

| 31 | 문서·파일 관련 질문 받음 | grep(대화 언급)/find(존재)/view(내용)/git(최신성)을 뒤섞어 쓰거나 하나만 쓰고 끝냄 | 질문을 4종 중 무엇인지 먼저 판정: "썼나/말했나"→grep, "있나"→find, "뭐라고 돼있나"→view, "최신인가"→git. 필요시 find→git→view 순서로 전부 적용 | 2026-07-21 실측(제수당/OR-001/thread-closing-review.md 사건, 동일 패턴 3회 반복) |

| 32 | 원문이 구조화 형식(JSON 등)과 비구조화 형식(마크다운 등) 둘 다 있음 | 마크다운 표본 grep만으로 "충분히 확인했다"고 판단, 구조화 원본은 안 엶 | JSON 등 구조화 원본이 있으면 먼저 파싱해 메타데이터(messageCount 등)로 완전성부터 기계 검증. 텍스트본은 내용 확인용으로만 사용 | 2026-07-22 실측(Claude/GPT 원문 JSON 파싱으로 455=455, 804노드 완전성 최초 증명) |

---

**교정표 등재 기준 (필터):** 이 규칙이 없으면 AI가 높은 확률로 잘못된
기본 행동을 선택하는가? 예 → 교정표. 아니오 → Handbook.

*상세 배경·Lifecycle·SOP·개념필터·규칙 제안 절차는
notes/default-correction-handbook.md 참고.
이 표는 "무엇을 할지"만 담고 "왜"는 담지 않는다.*